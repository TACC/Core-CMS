{
  "name": "@tacc/core-cms",
  "version": "3.15.0",
  "license": "MIT",
  "description": "Any custom code for all new and updated TACC CMS sites.",
  "scripts": {
    "build": "npm run build:css --project=$npm_config_project",
    "build:css": "bin/update-css-version.js && bin/build-css-via-cli.js"
  },
  "// scripts": {
    "build": "Run all build commands for Core and given project",
    "build:help": "The `$npm_config_` allows `npm run build --project=...`",
    "build:css": "Build stylesheets from source files for any project"
  },
  "repository": "git@github.com:TACC/Core-CMS.git",
  "dependencies": {
    "@tacc/core-styles": "tacc-wbomar/Core-Styles.git",
    "node-cmd": "^5.0.0"
  },
  "author": "TACC ACI WMA <wma-portals@gmail.com>"
}
